# Requirements Catalog

**Project:** Chainlink Bookmark Manager
**Source:** [vision.md](vision.md)
**Date:** 2026-04-13

---

## User Roles

| Role             | Description                                                                |
|------------------|----------------------------------------------------------------------------|
| User             | An individual who owns a collection and manages bookmarks, folders, and tags |
| Collection Owner | A User who created a collection and can share it with others               |

---

## Functional Requirements

| ID     | Title                       | User Story                                                                                                                           | Priority | Status |
|--------|-----------------------------|--------------------------------------------------------------------------------------------------------------------------------------|----------|--------|
| FR-001 | Auto-Provision on First Login | As a User, I want the system to automatically create a default collection for me the first time I identify myself, so that I can start saving bookmarks without any manual setup. | High     | Done   |
| FR-002 | Auto-Navigate to Collection    | As a User, I want the system to take me directly to my collection when I have only one collection or have set a default collection, so that I do not have to choose every time I open the application. | High     | Done   |
| FR-003 | List My Collections            | As a User, I want to see a list of all collections I have access to so that I can navigate to the right bookmark collection when I have more than one collection and no default is set. | High     | Done   |
| FR-004 | Set Default Collection         | As a User, I want to designate one collection as my default so that I am taken directly to it on every login without needing to select it. | Medium   | Done   |
| FR-005 | Create Bookmark             | As a User, I want to create a bookmark with a URL, title, and optionally a folder and tags so that I can save and organize web resources. | High     | Done   |
| FR-006 | View Bookmarks              | As a User, I want to view all bookmarks in my collection so that I can browse my saved resources.                                    | High     | Done   |
| FR-007 | Edit Bookmark               | As a User, I want to edit a bookmark's URL, title, description, folder, and tags so that I can keep my bookmarks up to date. | High     | Done   |
| FR-008 | Delete Bookmark             | As a User, I want to delete a bookmark so that I can remove resources I no longer need.                                              | High     | Done   |
| FR-009 | Create Folder               | As a User, I want to create a folder so that I can organize bookmarks into groups.                                                   | High     | Done   |
| FR-010 | View Folders                | As a User, I want to view all folders in my collection so that I can see my organizational structure.                                | High     | Done   |
| FR-011 | Rename Folder               | As a User, I want to rename a folder so that I can keep my organizational structure accurate.                                        | Medium   | Done   |
| FR-012 | Nest Folders                | As a User, I want to nest folders inside other folders so that I can create a hierarchy for my bookmarks.                            | Medium   | Done   |
| FR-013 | Move Bookmark to Folder     | As a User, I want to move a bookmark into a folder so that I can organize my bookmarks.                                              | High     | Done   |
| FR-014 | Delete Folder               | As a User, I want to delete a folder so that I can remove organizational structures I no longer need.                                | Medium   | Done   |
| FR-015 | Create Tag                  | As a User, I want to create a tag so that I can label bookmarks with custom categories.                                              | High     | Done   |
| FR-016 | View Tags                   | As a User, I want to view all tags in my collection so that I can see and manage my available labels.                                | High     | Done   |
| FR-017 | Edit Tag                  | As a User, I want to edit a tag's name and color so that I can correct or improve my labeling scheme.                                               | Medium   | Done   |
| FR-018 | Delete Tag                  | As a User, I want to delete a tag so that I can remove labels I no longer use.                                                       | Medium   | Done   |
| FR-019 | Apply Tag to Bookmark       | As a User, I want to apply one or more tags to a bookmark so that I can categorize and filter my bookmarks.                          | High     | Done   |
| FR-020 | Remove Tag from Bookmark    | As a User, I want to remove a tag from a bookmark so that I can correct miscategorized bookmarks.                                    | Medium   | Done   |
| FR-021 | Filter Bookmarks by Tag     | As a User, I want to filter bookmarks by tag so that I can quickly find bookmarks in a category.                                     | High     | Done   |
| FR-022 | Filter Bookmarks by Folder  | As a User, I want to filter bookmarks by folder so that I can browse bookmarks within a specific folder.                             | High     | Done   |
| FR-023 | Share Collection               | As a Collection Owner, I want to share my collection with another user so that we can collaborate on a shared bookmark collection.   | Medium   | Done   |
| FR-024 | Access Shared Collection       | As a User, I want to access a collection shared with me so that I can view and manage its bookmarks.                                 | Medium   | Done   |
| FR-025 | Customize Tag Color             | As a User, I want to change a tag's color so that I can visually distinguish tags in my collection.                                  | Low      | Done   |
| FR-026 | Revoke Collection Access        | As a Collection Owner, I want to revoke another user's access to my shared collection so that they can no longer view or modify its contents. | Medium   | Done   |
| FR-027 | Logout                           | As a User, I want to log out of the application so that my session is ended and my data is protected from unauthorized access. | High     | Done   |
| FR-028 | Switch Language                  | As a User, I want to switch the interface language between English and German so that I can use the application in my preferred language. | Low      | Done   |
| FR-029 | Navigate Folder Hierarchy        | As a User, I want to see a breadcrumb showing the path to the currently selected folder so that I can see where I am and quickly navigate to parent folders. | Medium   | Done   |
| FR-030 | Search Bar Keyboard Shortcut      | As a User, I want to focus the search bar by pressing a keyboard shortcut (⌘K on Mac, Ctrl+K on Windows/Linux, or /) so that I can quickly start searching without reaching for the mouse. | Low      | Done   |
| FR-031 | Import Browser Bookmarks          | As a User, I want to import bookmarks from Brave or Firefox so that I can easily migrate my existing bookmarks into Chainlink. | Low      | Done   |
| FR-032 | Search Bookmarks               | As a User, I want to search bookmarks within my collection by title, URL, and tag name so that I can quickly find a specific bookmark. When tags are selected, only bookmarks matching all selected tags are shown. When a folder is selected, results are further limited to bookmarks within that folder. All three filters (search query, selected tags, selected folder) are combined as AND conditions. | High     | Done   |
| FR-033 | Offline Mode (superseded by FR-044) | As a User, I want to browse and search my bookmarks while offline so that I can access my saved resources without an active network connection. The app should load all collection data in a single request and cache it client-side. Authentication handling for offline access needs to be investigated. | Low      | Open |
| FR-034 | Login with Google                | As a User, I want to sign in using my Google account so that I can access the application without creating separate credentials. The system should auto-provision a local user record from my Google profile on first login. | High     | Done   |
| FR-035 | Register Account                 | As a User, I want to register an account with my email and password so that I can create credentials to access the application. Passwords must be hashed with bcrypt and the email must be unique. | High     | Done   |
| FR-036 | Create Collection                | As a User, I want to create a new collection with a name so that I can organize bookmarks in separate workspaces beyond the auto-provisioned default. | High     | Done   |
| FR-037 | Edit Collection                  | As a Collection Owner, I want to rename my collection so that it accurately reflects its purpose. | Medium   | Done   |
| FR-038 | Delete Collection                | As a Collection Owner, I want to delete a collection I own so that I can remove collections I no longer need. Deletion must require typing the collection name to confirm, cascade-remove all bookmarks, folders, tags, and access grants, and reassign the default if needed. | High     | Done   |
| FR-039 | Add ArchUnit Tests               | As a Developer, I want to have automated ArchUnit tests that verify the project follows defined architectural rules (layering, naming, transactions) to prevent technical debt. | High     | Done |
| FR-040 | Error Feedback             | As a User, I want to receive clear and immediate feedback when an operation fails, distinguishing between authentication/session issues and general backend or network problems (e.g., backend unavailable, server error, validation error), so that I am informed about the root cause and can take appropriate action. | High     | Done |
| FR-041 | Session Data Cleanup       | As a User, I want the application to completely reset all internal state and cached data when I log out or switch accounts so that no information is leaked between different users sharing the same browser session. | High     | Done   |
| FR-042 | Adopt Form Library with Zod Schemas | As a Developer, I want to replace manual form validation with VeeValidate v4 backed by Zod schemas so that form handling is consistent, type-safe, and less error-prone across the frontend. All 12 forms have been migrated to use VeeValidate composables (`useForm`/`defineField`/`handleSubmit`) with `toTypedSchema`, unified `FormFieldCl` component for label+input+error display, `useFormDialog` composable for dialog lifecycle, i18n-aware validation messages via `v()` helper, and 82 unit tests covering all schemas. | Medium   | Done |
| FR-043 | Browser Extension          | As a User, I want a Chrome/Firefox browser extension that lets me save the current page as a bookmark to my Chainlink collection with a single click, choosing collection, folder, and tags from a popup. | Medium   | Done |
| FR-044 | Offline Mode (superseded by FR-052–FR-060) | As a User, I want to browse and search my bookmarks while offline. The app already loads all collection data in a single request (`GET /collections/{id}`) and caches it client-side in Pinia stores. A service worker should cache the app shell and API responses so the app is usable without a network connection. | Low      | Superseded |
| FR-052 | Disable Offline Caching | As a User, I want to be able to disable offline caching in the Settings dialog so that I can prevent my bookmark data from being stored locally in the browser. Offline caching is enabled by default. | Low | Done |
| FR-053 | Cache Collection Data | As a User, I want the system to automatically persist my collection data (bookmarks, folders, tags) to IndexedDB after every successful API response so that the cached data stays up to date while I am online. Caching is active by default and requires no user action. | High | Done |
| FR-054 | Browse Bookmarks Offline | As a User, I want to browse, search, and filter my cached bookmarks when the server is unreachable so that I can access my saved resources during server outages without any network dependency. | High | Done |
| FR-055 | Offline Visual Indicator | As a User, I want to see a clear visual indicator (banner) when I am offline so that I know the displayed data may be stale and write operations are unavailable. | Medium | Done |
| FR-056 | Offline Read-Only Mode | As a User browsing offline, I want all write operations (create, edit, delete, move, share) to be disabled with a clear explanation so that I understand the current limitations and do not lose work. | Medium | Done |
| FR-057 | Offline Authentication Bypass | As a User who was previously authenticated on this browser, I want the router to let me access the app shell and my cached collections without contacting the server so that I am not redirected to the login page during an outage. Offline authentication works by loading the cached `UserInfoJson` from IndexedDB (stored under `{email}:user-info`) when `GET /api/auth/me` fails due to network error. There is no password check or session cookie validation — the browser trusts its own local cache as evidence of a prior successful login. The user must have completed at least one successful online login in this browser before offline access is possible. The cached user identity (email) must be validated against the requested cache keys to prevent cross-user data leakage. | High | Done |
| FR-058 | Display Cache Freshness | As a User browsing offline, I want to see when the cached data was last synced (e.g., "Last synced 5 minutes ago") so that I can assess how current the displayed information is. | Low | Done |
| FR-059 | Auto-Recovery on Reconnect | As a User who comes back online, I want the app to automatically detect restored connectivity, refresh my collection data from the server, and dismiss the offline banner so that I seamlessly return to the live state. | Medium | Done |
| FR-060 | Purge Cache on Logout | As a User, I want all locally cached offline data to be purged from IndexedDB when I log out so that my bookmark data is not accessible to other users of the same browser. All cached data is keyed by the user's email address so that even if purge fails, another user cannot read it. | High | Done |
| FR-061 | Cache Data Isolation | As a User, I want my cached offline data to be isolated from other users who may use the same browser, so that no user can ever access another user's cached bookmarks. All IndexedDB entries must be scoped by the user's email (e.g., key format `{email}:collections`, `{email}:collection-info:{collectionId}`, `{email}:user-info`). The offline middleware must validate that the user identity embedded in the cache key matches the authenticated (or last-cached) user before serving any cached data. | High | Done |
| FR-062 | Offline Error Classification | As a User, I want the offline fallback to activate only when the server is genuinely unreachable due to network failure, not when the server responds with an error or rejects requests due to misconfiguration. The offline middleware must classify errors before serving cached data: only `TypeError` from `fetch()` (network-level failure) on `GET` requests to known cacheable endpoints (`/api/auth/me`, `/api/collections`, `/api/collections/{id}`) when `navigator.onLine === false` may trigger the offline fallback. HTTP errors (401, 403, 500), `AbortError`, non-GET requests, requests while `navigator.onLine === true`, and unknown URL patterns must never trigger the offline fallback — they must propagate normally. | High | Done |
| FR-045 | Drag and Drop Reorganization | As a User, I want to drag and drop folders and bookmarks (excluding the root folder) to reorganize them within my collection, so that I can intuitively move items between folders, nest folders inside other folders, and reorder items without using context menus. | Medium   | Done   |
| FR-046 | Trashbin for Deleted Items   | As a User, I want deleted bookmarks and folders to be moved to a trashbin instead of being permanently removed, so that I can recover accidentally deleted items. Deleting a folder also moves all its contained bookmarks and subfolders to the trashbin. | High     | Done   |
| FR-047 | View Trashbin                | As a User, I want to access the trashbin through the user menu so that I can see all my soft-deleted bookmarks and folders and decide whether to restore or permanently delete them. | High     | Done   |
| FR-048 | Restore from Trashbin        | As a User, I want to restore a deleted bookmark or folder from the trashbin to its original location so that I can recover items I accidentally deleted. | Medium   | Done   |
| FR-049 | Permanently Delete from Trashbin | As a User, I want to permanently delete individual items from the trashbin or empty the entire trashbin so that I can free up space and irreversibly remove items I no longer need. | Medium   | Done   |
| FR-050 | Review Cleanup Suggestions | As a User, I want to see a list of bookmarks I haven't used in over X months and be able to move selected ones to the trashbin or dismiss suggestions, so that I can declutter my collection based on actual usage data. Dismissed suggestions will not reappear for the same duration as the inactivity threshold. | Medium   | Done |
| FR-051 | Auto-Tag Bookmarks by URL Pattern | As a User, I want the system to suggest tags automatically when I create or edit a bookmark based on the URL I enter, so that my bookmarks are consistently tagged without manual effort. Suggestions must appear in a dedicated "Suggested Tags" section (separate from the real tag dropdown) as toggleable chips. Each suggestion can be individually selected or deselected. Clicking "Accept Suggestions" moves the selected tags into the bookmark's actual tag selection. Suggestions are not applied unless explicitly accepted. The system should infer tags from URL patterns such as subdomains (e.g., `dev.myproject.com` → `dev`, `uat.myproject.com` → `uat`) and other configurable domain-based rules. | Low      | Done   |
| FR-063 | Fetch Bookmark Favicons | As a User, I want bookmarks to display their site favicons next to the title so that I can scan my list visually. The frontend must dispatch favicon loading based on a per-collection internal-domain allowlist: hosts matching the allowlist are loaded directly by the browser via an `<img>` tag (so internal/intranet bookmarks resolve from the user's network); all other hosts are loaded through a backend proxy `GET /api/collections/{collectionId}/bookmarks/{bookmarkId}/favicon` that enforces SSRF protections (private/loopback/link-local/metadata IPs blocked, redirects re-validated, schemes restricted to http/https, content-type `image/*`, size and timeout caps). Cached favicons are stored on the local filesystem under a configurable cache directory (default `developer-local-settings/favicon-cache/`), keyed by the SHA-256 hash of the canonical bookmark origin, with a sidecar holding `contentType`/`fetchedAt`/negative-cache marker; entries expire after a configurable TTL (default 30 days success / 6 hours negative) and are not cascaded on bookmark or collection deletion since cache entries are origin-keyed and shared. Failed fetches fall back to a generic placeholder. The allowlist is stored on the `Collection` entity, editable only by the Collection Owner, and shared with everyone who has access to the collection. | Medium   | Done   |
| FR-064 | Favicon Cache Cleanup Job | As an Operator, I want the favicon cache directory (FR-063) to stay within a configurable size budget (default 40 MB) so that long-running deployments do not accumulate unbounded disk usage. A scheduled job runs once per week (configurable cron, default Sunday 03:00). When the cache directory exceeds the threshold, the job iterates bookmarks across all collections in ascending creation-timestamp order, skips any bookmark younger than a configurable minimum age (default 4 weeks), and deletes the origin-keyed cache files for each eligible bookmark until the threshold is met. The job only deletes files inside the favicon cache directory — it never deletes bookmarks, collections, or any database entity. The schedule, threshold, minimum age, and an enabled/disabled flag are application properties. | Low      | Done   |
| FR-065 | View Bookmark Screenshot Previews | As a User, I want to see a screenshot preview of each bookmarked page so that I can visually identify bookmarks at a glance. The feature is toggleable per collection (off by default) by the Collection Owner. When enabled, a new "tiles" layout option becomes available that displays bookmarks as a grid of cards with screenshot cover images. Screenshots are captured asynchronously by a Playwright-based sidecar service and served from an on-disk cache. The screenshot capture runs entirely outside the main bookmark CRUD path and never delays bookmark creation or editing. | Medium   | Done |
| FR-066 | Screenshot Cache Cleanup Job | As an Operator, I want the screenshot cache directory (FR-065) to stay within a configurable size budget (default 200 MB) so that long-running deployments do not accumulate unbounded disk usage. A scheduled job runs on a configurable cron schedule and evicts the oldest cache entries when the budget is exceeded. | Medium   | Done |
| FR-069 | Bookmark Properties | As a User, I want bookmarks to support typed key-value properties so that I can attach structured metadata (e.g., priority, reading status) without abusing tags. Properties are defined at the collection level with a name, type (text, date, select, multi-select, boolean, number), and optional allowed values. Each bookmark can have zero or more property values. Property values are editable in the bookmark create and edit dialogs. Properties are not displayed as tag pills — they appear in a dedicated section on the bookmark detail or card. | High | Done |
| FR-091 | Collapsible Properties Section in Edit Bookmark Dialog | As a User editing a bookmark in a collection with many properties, I want the Properties section to collapse when there are 6 or more property definitions so that the dialog remains manageable. When collapsed, a "N filled" hint tells me how many properties already have values so I know what is inside without opening the section. My last expanded/collapsed state is remembered per collection. Collections with 5 or fewer properties always show the section fully expanded with no toggle. | Medium | Done |
| FR-070 | Filter Bookmarks by Property | As a User, I want to filter my bookmarks by property values so that I can quickly find all bookmarks with a specific import source, priority level, or reading status. Property filters combine with existing tag and folder filters using AND logic. | Medium | Done |
| FR-071 | Search Operator Language | As a User, I want to type structured search operators in the search bar (e.g., `#tag`, `imported:2026-04`, `created:>2026-01`, `folder:recipes`, `-#deprecated`) so that I can perform precise queries without navigating through filter dialogs. | Medium | Done |
| FR-072 | Search Operator Autocomplete | As a User, I want the search bar to suggest available operators, tag names, property values, and folder names as I type so that I can discover and use the search operator language without memorizing syntax. The autocomplete dropdown must appear when the user types a trigger character (`#` for tags, `propertyname:` for properties, `folder:` for folders) and show matching items from the current collection. | Medium | Done |
| FR-073 | Search Operator: Negation | As a User, I want to prefix any search operator with `-` to exclude results so that I can find bookmarks that do NOT match a tag, property, or condition (e.g., `-#deprecated`, `-imported:true`). | Medium | Done |
| FR-074 | Search Operator: Date Ranges | As a User, I want to use date operators (`created:YYYY-MM-DD`, `created:>YYYY-MM-DD`, `created:<YYYY-MM-DD`) in the search bar so that I can find bookmarks by creation date without a separate date picker. Relative dates (`created:>today-30d`) must also be supported. | Low | Done |
| FR-075 | Saved Searches | As a User, I want to save a search query (including operators) with a name so that I can re-run it later without retyping the full query. Saved searches are scoped to the collection they were created in. | Medium | Done |
| FR-076 | Smart Collections | As a User, I want saved searches to appear in the sidebar as virtual folders so that I can access them with a single click. All saved searches are always shown in the sidebar — there is no separate "pin" action. Smart collections display the live result set of their underlying query — they always reflect the current bookmark data and never store a static snapshot. | Medium | Done |
| FR-077 | Manage Saved Searches | As a User, I want to rename, update the query, and delete my saved searches so that I can keep my sidebar organized. Renaming is done via the row's context menu. Query updates are done inline: when an active smart collection's query is modified, a pill shows an "Update" button to persist the new query. Deleting a smart collection must not delete the bookmarks it matches. | Low | Done |
| FR-078 | Multi-Select Bookmarks | As a User, I want to select multiple bookmarks at once (via checkboxes or Shift/Ctrl+Click) so that I can perform batch operations on them. The selected count must be visible in a toolbar that appears when items are selected. | Medium | Open |
| FR-079 | Batch Tag Application | As a User, I want to add or remove tags from multiple selected bookmarks at once so that I can categorize large groups of bookmarks efficiently. A batch tag dialog must show all existing tags as toggleable options. | Medium | Open |
| FR-080 | Batch Move and Delete | As a User, I want to move multiple selected bookmarks to a folder or delete them in one action so that I can reorganize or clean up my collection efficiently. Batch delete must move bookmarks to the trashbin (not permanently delete). | Medium | Open |
| FR-081 | Bookmark Notes | As a User, I want to add a personal note to any bookmark so that I can remember why I saved it or add context that the page title and description don't capture. Notes are private (not shared with other collection members), support plain text up to 10,000 characters, and are searchable via the search bar and search operators (`note:keyword`). | Medium | Open |
| FR-082 | Sort Preferences (per collection) | As a User, I want to choose how bookmarks are sorted (by title, creation date, last clicked date, or click count) and have my preference remembered per collection so that each collection opens in my preferred sort order. Collections with no explicit preference fall back to the system default (creation date descending). Note: "Last clicked" and "Click count" reflect *aggregate* click activity across all members of a shared collection, not per-user activity. | Low | Done |
| FR-083 | Manage API Keys | As a User, I want to create, list, and revoke personal API keys through the web UI so that I can authenticate non-browser clients (CLI, scripts) against the Chainlink API without sharing my session cookie or Google credentials. Each API key has a name for identification, a non-guessable secret shown only once at creation, and can be revoked individually at any time. | High | Open |
| FR-084 | Authenticate via API Key | As a CLI User or script, I want to authenticate API requests by sending an `X-API-Key` header so that I can access all bookmark and collection endpoints that I could normally access through the browser UI. The API key must be equivalent to a session — it inherits all roles and collection access grants of the owning user. | High | Open |
| FR-085 | Manage Bookmarks via CLI | As a User, I want to manage my bookmarks from the command line (`chainlink bookmarks add <url>`, `list`, `edit`, `rm`) so that I can save, search, and organize bookmarks from a terminal or shell script without opening a browser. The CLI authenticates via an API key (FR-084) and communicates exclusively through the existing HTTP API. | Medium | Open |
| FR-086 | CLI Login Flow | As a User, I want to configure my CLI credentials by running `chainlink login` and pasting an API key (or providing it via `--api-key` flag or `CHAINLINK_API_KEY` environment variable) so that subsequent CLI commands are automatically authenticated. The API key is stored locally in a config file (`~/.chainlink/config.json`). | Medium | Open |
| FR-087 | Extension Property Support | As a User, I want the browser extension (FR-043) to display and set bookmark properties (FR-069) when saving a new bookmark so that I can attach structured metadata (e.g., priority, reading status) directly from the extension popup without opening the web app. The extension must render the appropriate input widget for each property type (text, number, date, select, multi-select, boolean) and persist the values via the existing `PUT /bookmarks/{id}/properties` endpoint after bookmark creation. When the collection has no property definitions, the Properties section is hidden. | Medium | Open |
| FR-088 | Application Metrics Collection | As an Operator, I want the backend to collect and expose application-level metrics (HTTP request counts and latencies per endpoint, JVM memory and GC stats, database connection pool usage, scheduled job execution counts and durations, and custom business counters such as total bookmarks per collection and active user sessions) via a vendor-neutral instrumentation facade (Micrometer) so that I can monitor system health, diagnose performance bottlenecks, and plan capacity without modifying application code to switch monitoring backends. Metrics must be registered at application startup using Quarkus/Micrometer integrations — no manual registry management is required. | High | Done |
| FR-089 | Metrics Export Endpoint | As an Operator, I want the collected metrics (FR-088) to be exposed on a dedicated, configurable HTTP endpoint (default `/q/metrics`) in a format compatible with common observability stacks (e.g., Prometheus exposition format, OTLP) so that an external metrics scraper or push gateway can ingest them. The export format must be selectable via application configuration without code changes. The endpoint must be accessible without authentication when the operator has configured it for an internal/trusted network, or secured via the existing authentication mechanisms when exposed externally. | High | Done |
| FR-090 | Metrics Dashboard | As an Operator, I want to connect the metrics export endpoint (FR-089) to an external dashboarding tool (e.g., Grafana) so that I can visualize system health, request rates, error rates, and business metrics over time. The dashboard configuration (JSON model or equivalent) must be provided as a ready-to-import artifact in the project repository so that operators can set up monitoring with minimal manual configuration. | Medium | Done |
| FR-091 | PWA Feasibility Study | As a Product Owner, I want a feasibility study evaluating whether the Chainlink web app can be delivered as a Progressive Web App (installable home-screen app with offline support and push notifications) so that we can decide whether to invest in a PWA implementation or continue distributing through browser extensions and a planned native CLI only. The study must cover: (1) installability requirements (manifest, icons, HTTPS, service-worker registration) and the gap versus the current Vite build; (2) which features can work offline (browsing cached bookmarks, queued create/edit) and which cannot (sharing, collaborator updates); (3) auth implications when running as a standalone PWA outside the browser's normal cookie context; (4) impact on the existing browser extensions (overlap vs. complement); (5) effort estimate and recommended next steps. Deliverable: [pwa-feasibility.md](pwa-feasibility.md). | Low | Done |

---

## Non-Functional Requirements

| ID      | Title                   | Requirement                                                                                          | Category     | Priority | Status |
|---------|-------------------------|------------------------------------------------------------------------------------------------------|--------------|----------|--------|
| NFR-001 | Page Load Time          | All page loads must complete within 2 seconds under normal load (up to 10 concurrent users).         | Performance  | High     | Open   |
| NFR-002 | API Response Time       | All API responses must be returned within 500 ms under normal load (up to 10 concurrent users).      | Performance  | High     | Open   |
| NFR-003 | Access Control          | A user must not be able to read or modify resources belonging to a collection they have no access to; violations must return HTTP 403. | Security     | High     | Done   |
| NFR-004 | Audit Coverage          | All create, update, and delete operations on Bookmark, Folder, and Tag entities must be automatically tracked using Hibernate Envers; no custom audit table is required. | Auditability | Low      | Done   |
| NFR-005 | Code Coverage           | Service and repository classes must maintain a minimum of 80% line coverage as measured by the CI test run. | Testability  | Medium   | Open   |
| NFR-006 | Use Case Test Coverage  | Every functional requirement must be traceable to at least one automated integration test that verifies the main success scenario. | Testability  | Medium   | Open   |
| NFR-007 | Architectural Enforcement | Architectural constraints (layering, naming conventions, transaction boundaries) must be automatically enforced via ArchUnit tests. | Maintainability | High | Done |
| NFR-008 | Error Visibility        | 100% of failed API requests (HTTP 4xx/5xx) and network connectivity issues must be intercepted and reported to the user through a visible UI notification (e.g., toast). The system must specifically distinguish between login/authentication errors (401/403) and general backend problems (500+, network failures) to provide accurate feedback. | Usability | High | Done |
| NFR-009 | State Isolation         | Upon logout or session change, 100% of Pinia stores and sensitive local/session storage entries must be cleared. It must be impossible for a new user to see data from the previous user's session without a fresh API fetch. | Security | High | Done |
| NFR-010 | E2E Pipeline Execution | The Playwright E2E tests for the Chrome project must execute successfully as part of the continuous integration (CI) pipeline for every pull request and merge to the main branch. | Testability | High | Done |
| NFR-011 | Offline App Shell Load | When the service worker is active and the app shell is cached, the application must render the UI from the service worker cache within 500 ms on a cold page load with no network, as measured by Lighthouse offline PWA audit. | Performance | Medium | Done |
| NFR-012 | Offline Data Retrieval | When the server is unreachable and IndexedDB contains cached collection data, the offline middleware must serve the cached response to the Pinia store within 200 ms (no network round-trip). | Performance | Medium | Done |
| NFR-013 | Cache Storage Footprint | The total IndexedDB storage for offline cached data must not exceed 50 MB per user session (covering collections list + up to 10 collection infos with 1,000 bookmarks each). | Scalability | Low | Done |
| NFR-014 | Favicon Proxy SSRF Hardening | The backend favicon proxy (FR-063) must reject any URL whose host resolves to a loopback, private, link-local, or cloud-metadata IP range, must re-validate the resolved IP after every redirect, must enforce a fetch timeout (≤ 5 s), a maximum response size (≤ 256 KiB), a `Content-Type: image/*` allowlist, and must restrict accepted URL schemes to `http` and `https`. Internal proxy errors must not be echoed verbatim to the client. | Security | High | Done
| NFR-015 | Screenshot Proxy SSRF Hardening | The screenshot fetcher (FR-065) must apply the same SSRF guards as the favicon proxy (NFR-014): reject private/loopback/link-local/metadata IPs, enforce `http`/`https` only, apply timeouts (≤ 15 s), and never echo internal errors to the client. | Security | High | Done |
| NFR-016 | Search Operator Parse Performance | The search operator parser (FR-071) must parse and evaluate any valid query against 1,000 bookmarks in under 50 ms on the client side. Invalid queries must produce a user-visible error within 100 ms. | Performance | Medium | Done |
| NFR-017 | Property Query Performance | Filtering bookmarks by property values (FR-070) must return results within 500 ms for collections containing up to 10,000 bookmarks with up to 20 property definitions. | Performance | Medium | Done |
| NFR-018 | Batch Operation Atomicity | Batch operations (FR-079, FR-080) must be atomic: either all selected bookmarks are updated, or none are. Partial failures must roll back and display an error to the user. | Reliability | Medium | Open |
| NFR-019 | Saved Search Evaluation Consistency | Smart collections (FR-076) must return identical results whether the underlying query is evaluated client-side (for small collections) or server-side (for large collections). The evaluation strategy must be transparent to the user. Currently only client-side evaluation is implemented (consistent with C-015); since there is a single evaluation strategy, consistency is inherent. Server-side evaluation may be introduced later for large collections (≥ 10,000 bookmarks). | Consistency | Low | Done |
| NFR-020 | API Key Storage Security | API keys (FR-083) must never be stored in plaintext. The database stores only a SHA-256 hash of the key. The raw key is shown to the user exactly once (at creation time) and cannot be retrieved afterward. Key lookup on incoming requests uses constant-time comparison to prevent timing attacks. | Security | High | Open |
| NFR-021 | API Key Rotation Support | A user must be able to create up to 10 active API keys simultaneously to support key rotation without downtime. Creating an 11th key must fail with a clear error message indicating the limit. | Security | Medium | Open |
| NFR-022 | CLI API Client Generation | The CLI must use a TypeScript client generated from the same OpenAPI spec as the frontend (`/q/openapi`) so that type-safety is guaranteed and API changes are detected at build time. | Maintainability | High | Open |
| NFR-023 | CLI Distribution | The CLI must be installable via `npm install -g @chainlink/cli` and require no runtime dependencies beyond Node.js ≥ 20. | Usability | Medium | Open |
| NFR-024 | Metrics Cardinality Budget | The total number of unique metric time series (tag combinations) emitted by FR-088 must not exceed 500 per application instance. High-cardinality dimensions (e.g., bookmark IDs, user emails) must not be used as metric tags. Only low-cardinality dimensions (HTTP method, status code class, endpoint path template, job name) are permitted. Exceeding the cardinality budget must log a warning at startup. | Scalability | Medium | Done |
| NFR-025 | Metrics Endpoint Response Time | The metrics export endpoint (FR-089) must respond within 500 ms under normal load. Metric collection and serialization must not block or degrade the latency of business API endpoints. | Performance | High | Done |
| NFR-026 | Metrics Collection Overhead | Enabling metrics collection (FR-088) must add no more than 5% overhead to the p99 latency of any API endpoint, as measured by a load test with 10 concurrent users. | Performance | High | Done |
| NFR-027 | Test Structure (Arrange/Act/Assert) | Every test method (backend unit and integration tests, frontend unit tests) must be organized into three clearly delimited sections marked with the comments `// ARRANGE`, `// ACT`, and `// ASSERT` (in that order). The ACT section must contain exactly one statement — the invocation under test. No test logic may appear outside these three sections. Compliance is verified by code review and an automated check that rejects any test method missing one or more of the three section markers. | Maintainability | High | Open |
| NFR-028 | SQLite Concurrency via WAL Mode | The feasibility of switching the SQLite database (C-003) from the default rollback-journal mode to [Write-Ahead Logging (WAL)](https://www.sqlite.org/wal.html) must be investigated and documented. WAL enables concurrent readers alongside a single writer, significantly reducing `SQLITE_BUSY` contention under multi-user and background-job workloads (e.g., favicon/screenshot cleanup jobs, metrics, batch operations). The investigation must cover: (1) enabling WAL via a `PRAGMA journal_mode=WAL` migration or Hibernate/Quarkus datasource configuration; (2) impact on Flyway migrations (WAL persists across connections, so a one-time migration is preferred over per-connection pragmas); (3) concurrency behavior with the Agroal connection pool (quarkus-datasource) — readers must not block on a writer's transaction and `SQLITE_BUSY` must be mitigated with a configurable busy timeout; (4) durability and crash-recovery characteristics (WAL checkpoint strategy: `PASSIVE` vs `FULL` vs `TRUNCATE`) and whether synchronous mode can be relaxed (`NORMAL` vs `FULL`); (5) backup implications (WAL requires `.shm` and `-wal` sidecar files — the backup procedure must capture them together with the main `.db` file); (6) network filesystem caveat (WAL does not work reliably on NFS/network-mounted volumes, so the deployment guide must state this). The deliverable is a short feasibility report (e.g., `docs/sqlite-wal-feasibility.md`) with a recommendation and, if positive, an implementation plan. **Note:** This NFR is an investigation-spurring placeholder — it may be removed (or converted into a concrete implementation NFR) once the feasibility report is complete, as it holds no long-term value beyond driving the exploration. | Scalability | Medium | Open |

---

## Constraints

| ID    | Title              | Constraint                                                                 | Category  | Priority | Status |
|-------|--------------------|----------------------------------------------------------------------------|-----------|----------|--------|
| C-001 | Backend Framework  | Backend must be implemented using Quarkus.                                 | Technical | High     | Done   |
| C-002 | Frontend Framework | Frontend must be implemented using Vue.js with Pinia for state management. | Technical | High     | Done   |
| C-003 | Single Database    | All collections, bookmarks, folders, tags, and access control must be stored in a single SQLite database file. Multi-tenancy is achieved via `collection_id` foreign keys. (Note: Multi-database architecture may be considered for future scaling.) | Technical | High     | Done   |
| C-004 | Deployment Model   | The application must be deployable as a self-hosted installation without external cloud dependencies. | Technical | High     | Done   |
| C-005 | User Identity      | Users must be identified by a stable, unique username. Form-based authentication with bcrypt password hashing and Google OIDC are both supported. The `email` claim serves as the principal for both providers. | Technical | High     | Done   |
| C-006 | Primary Keys       | All entity primary keys must be UUIDs generated server-side; auto-increment sequences must not be used. | Technical | High     | Done   |
| C-007 | Out of Scope       | User management (creating, editing, deleting user accounts by an admin) is out of scope for this release. | Business  | High     | Done   |
| C-008 | UI Layout          | The frontend must follow the layout and design system defined in [ui-layout.md](ui-layout.md), including the three-column desktop layout, sidebar components, and shadcn/vue component library with Tailwind CSS. | Technical | High     | Done   |
| C-009 | Offline Auth Trust Model | Offline access relies on a cached `UserInfoJson` in the browser's IndexedDB as proof of prior authentication. There is no password re-check, no session cookie validation, and no server contact. This is an accepted security trade-off: the cached data is the same data the user already saw on screen during their last online session. The cache is protected by same-origin policy and user-scoped keys (`{email}:*`). **Known edge case accepted:** if User A closes the browser tab without logging out, and User B opens the app on the same browser while the server is down, User B will see User A's cached bookmarks (read-only). This risk is deemed acceptable for a self-hosted bookmark manager. | Security | High     | Done   |
| C-010 | Offline Requires Prior Login | Offline mode can only work if the user has completed at least one successful online login in this browser. Users who have never logged in, or who explicitly logged out before the outage, will be redirected to the login page (which requires network). | Technical | High     | Done   |
| C-011 | Favicon Cache Storage | The backend favicon cache (FR-063) must be stored on the local filesystem under a configurable directory (default `developer-local-settings/favicon-cache/`), not in the SQLite database. Cache files are named by SHA-256 of the canonical bookmark origin. The directory may be deleted at any time without data loss; it will be re-populated lazily on demand. This keeps C-003 (single SQLite database) free of large opaque BLOBs and lets operators size, mount, or wipe the cache independently of application backups. | Technical | Medium | Done |
| C-012 | Screenshot Cache Storage | The backend screenshot cache (FR-065) must be stored on the local filesystem under a configurable directory (default `developer-local-settings/screenshot-cache/`), not in the SQLite database. The directory may be deleted at any time without data loss; it will be re-populated lazily on demand. This follows the same rationale as C-011. | Technical | High | Done |
| C-013 | Screenshot Sidecar Service | Screenshot capture (FR-065) must be performed by a Playwright-based microservice running as a separate Docker container (sidecar), not embedded in the Quarkus JVM. The Quarkus API communicates with the sidecar over HTTP. This isolation prevents Chromium native dependencies from affecting the main application and keeps the screenshot service independently scalable and replaceable. | Technical | High | Done |
| C-014 | Properties Stored in Database | Bookmark properties (FR-069) must be stored in the SQLite database using a key-value join table (not in a JSON column or external store), consistent with the existing entity-relational pattern used for tags. This enables SQL-level filtering (FR-070) and keeps the schema explicit and queryable. | Technical | High | Done |
| C-015 | Search Operators are Client-Side | The search operator language (FR-071) must be evaluated entirely client-side using data already loaded in the Pinia store, consistent with the existing search architecture (FR-032). Server-side search evaluation may be introduced later for large collections (≥ 10,000 bookmarks) but must not be the initial implementation. | Technical | Medium | Done |
| C-016 | Import Metadata via Properties | ~~Import source tracking (FR-067) must use the bookmark properties system (FR-069), not tags.~~ FR-067 and FR-068 have been removed — the import process no longer generates `imported=` tags. The original creation date from the imported file is preserved as the bookmark's `timestampErstellt`. | Technical | High | Done |
| C-017 | Batch Size Limit | Batch operations (FR-078–FR-080) must support selecting and processing up to 500 bookmarks in a single batch. Batches exceeding 500 items must be rejected with a validation error (HTTP 400) rather than chunked — the caller must reduce the selection below the limit and retry. | Technical | Medium | Open |
| C-018 | API Key Authentication Mechanism | API key authentication (FR-084) must be implemented as a custom Quarkus `HttpAuthenticationMechanism` that runs alongside the existing OIDC and form-based mechanisms. The mechanism must check for an `X-API-Key` header on every request, resolve the hashed key to a user, and build a `SecurityIdentity` with the user's roles. If no `X-API-Key` header is present, the mechanism falls through to the next auth provider (OIDC/form). This preserves the existing browser-based authentication flow without modification. | Technical | High | Open |
| C-019 | CLI Lives in Monorepo | The CLI source code must live in a `cli/` directory at the root of the Chainlink repository, alongside `api/` and `frontend/`. This ensures API contract changes are detected at build time, version alignment is trivial (single git tag), and the generated TypeScript client is always in sync with the OpenAPI spec. | Technical | High | Open |
| C-020 | CLI Language is TypeScript | The CLI must be implemented in TypeScript, reusing the same `typescript-fetch` client generated from the OpenAPI spec that the frontend uses. This avoids maintaining a second code-generation pipeline and keeps the team in a single language ecosystem. | Technical | High | Open |
| C-021 | Metrics via Micrometer | Application metrics (FR-088) must be instrumented using the Micrometer library integrated with Quarkus (`quarkus-micrometer`). Micrometer provides a vendor-neutral facade so that the metrics backend (Prometheus, OTLP, Influx, etc.) can be swapped via configuration without code changes. Custom business metrics must use `@Counted`, `@Timed`, or manual `MeterRegistry` injection — no direct dependency on a specific monitoring backend's client library. | Technical | High | Done |
| C-022 | Observability Stack is Pluggable | The choice of external observability stack (Prometheus + Grafana, Grafana Loki, Datadog, etc.) must not be dictated by the application. The application only exposes metrics through the Micrometer abstraction (C-021) and a configurable export endpoint (FR-089). Operators choose their own scraper, storage, and visualization stack. The project may provide optional example configurations (e.g., a `docker-compose.monitoring.yml` with Prometheus + Grafana) but these are reference material, not required infrastructure. | Technical | High | Done |
