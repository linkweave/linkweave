# Both spellings kept deliberately (same pattern as frontend/):
# onlyBuiltDependencies for pnpm <= 10, allowBuilds for pnpm >= 11.
onlyBuiltDependencies:
  - esbuild
  - sharp

allowBuilds:
  esbuild: true
  sharp: true

verifyDepsBeforeRun: false
